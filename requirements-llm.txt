# Script writer: backend "local" (GGUF chạy qua llama.cpp) + client API
# OpenAI-compatible (backend "cerebras"). Chỉ cần khi stage script chạy thật.
llama-cpp-python>=0.3.9
huggingface_hub[hf_transfer]>=0.27.0
openai>=1.50.0
